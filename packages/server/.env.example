# Server environment variables (optional — defaults work for local guest play)

# PORT=3001
# CLIENT_URL=http://localhost:5173
# ADMIN_SECRET=ADMIN$

# Optional Supabase (match history / verify access tokens). Never expose service_role to the client.
# See supabase/README.md
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
