{
  "name": "shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:splendor-deck": "pnpm build && node -e \"import('./dist/splendor-deck.js').then((m) => { const e = m.validateSplendorDevelopmentDeck(); if (e.length) { console.error(e); process.exit(1); } console.log('splendor-deck ok'); })\""
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
